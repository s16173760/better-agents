# Example: Generated Project Structure

This document shows what a project generated by Better Agents looks like.

## Example: Stock Trading Agent (Python + Agno)

When you run:
```bash
better-agents init stock-trading-agent
# Select: Python, Agno, Claude Code, OpenAI
# Goal: "Build an agent that analyzes stock prices and provides recommendations"
```

You get this structure:

```
stock-trading-agent/
│
├── app/                                # Main application code
│   └── main.py                        # Entry point with basic structure
│
├── prompts/                           # Versioned prompt files
│   ├── sample_prompt.yaml            # Example prompt structure
│   └── [your prompts here]
│
├── tests/                            # All testing
│   ├── evaluations/                  # Jupyter notebooks for evaluations
│   │   └── example_eval.ipynb       # Sample evaluation notebook
│   │
│   └── scenarios/                    # End-to-end scenario tests
│       └── example_scenario.test.py # Sample scenario test
│
├── .cursorrules                      # Agno coding standards (auto-fetched)
├── llms.txt                          # Agno documentation (auto-fetched)
├── .mcp.json                         # MCP server configuration
├── AGENTS.md                         # Your development guidelines
├── prompts.json                      # Prompt registry
├── .env                              # Your API keys (DO NOT COMMIT)
├── .env.example                      # Template for environment variables
└── .gitignore                        # Proper gitignore
```

### Key Files Content

#### `.mcp.json`
```json
{
  "mcpServers": {
    "langwatch": {
      "command": "npx",
      "args": [
        "-y",
        "@langwatch/mcp-server",
        "--apiKey=sk-lw-..."
      ]
    }
  }
}
```

#### `AGENTS.md` (excerpt)
```markdown
# Agent Development Guidelines

## Project Overview

**Goal:** Build an agent that analyzes stock prices and provides recommendations

**Framework:** Agno
**Language:** Python

## Core Principles

### 1. Agent Testing Pyramid
- Unit Tests (Foundation)
- Evals & Optimization (Middle Layer)
- Simulations (Peak)

### 2. Test Every Feature
CRITICAL: Every new agent feature MUST be tested...

[... comprehensive guidelines ...]
```

#### `app/main.py`
```python
"""
Main entry point for your agent.
"""

def main():
    print("Welcome to your agent!")
    # TODO: Implement your agent logic here

if __name__ == "__main__":
    main()
```

#### `prompts/sample_prompt.yaml`
```yaml
# Sample prompt for your agent
model: gpt-4o
temperature: 0.7
messages:
  - role: system
    content: |
      You are a helpful AI assistant.
```

#### `.env` (with your keys)
```bash
# LLM Provider API Keys
OPENAI_API_KEY=sk-...

# LangWatch
LANGWATCH_API_KEY=sk-lw-...
```

---

## Example: Customer Support Agent (TypeScript + Mastra)

When you run:
```bash
better-agents init customer-support
# Select: TypeScript, Mastra, Claude Code, OpenAI
# Goal: "Build a customer support agent"
```

You get this structure:

```
customer-support/
│
├── src/                              # Main application code (TypeScript)
│   └── index.ts                     # Entry point
│
├── prompts/                         # Versioned prompt files
│   ├── sample_prompt.yaml
│   └── [your prompts here]
│
├── tests/
│   ├── evaluations/
│   │   └── example_eval.ipynb
│   │
│   └── scenarios/
│       └── example_scenario.test.ts # TypeScript test file
│
├── .mcp.json                        # MCP with Mastra + LangWatch
├── AGENTS.md                        # Guidelines (Mastra-specific)
├── prompts.json
├── .env
├── .env.example
└── .gitignore
```

### Key Differences

#### `.mcp.json` (includes Mastra)
```json
{
  "mcpServers": {
    "langwatch": {
      "command": "npx",
      "args": ["-y", "@langwatch/mcp-server", "--apiKey=sk-lw-..."]
    },
    "mastra": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@mastra/mcp-docs-server"]
    }
  }
}
```

#### `AGENTS.md` (Mastra guidance)
```markdown
### Mastra Framework

**Always use the Mastra MCP for learning:**

- The Mastra MCP server provides real-time documentation
- Ask it questions about Mastra APIs and best practices

**Example questions for Mastra MCP:**
- "How do I create an agent in Mastra?"
- "What's the best way to handle tools in Mastra?"
```

---

## What Makes This Production-Ready?

### ✅ Testing Infrastructure
- Evaluation notebooks in `tests/evaluations/`
- Scenario tests in `tests/scenarios/`
- Follows Agent Testing Pyramid

### ✅ Prompt Management
- All prompts in `prompts/` directory
- Versioned with YAML
- Never hardcoded

### ✅ Framework Best Practices
- **Agno**: .cursorrules + llms.txt
- **Mastra**: MCP for real-time docs

### ✅ LangWatch Integration
- MCP for expert guidance
- Prompt CLI ready
- Scenario testing setup
- Evaluation notebooks

### ✅ Professional Setup
- Proper .gitignore
- Environment variables
- Documentation (AGENTS.md)
- Clear structure

---

## After Generation

Your coding assistant (e.g., Claude Code) can:

1. ✅ **Read AGENTS.md** - Understand project guidelines
2. ✅ **Use Framework MCP** - Learn Agno/Mastra best practices
3. ✅ **Use LangWatch MCP** - Get help with prompts and testing
4. ✅ **Follow Structure** - Know where to put code
5. ✅ **Write Tests** - Know how to test properly

The assistant becomes an **expert** in your chosen framework AND LangWatch best practices!

---

## Starting Development

```bash
cd stock-trading-agent
claude "Build an agent that analyzes stock prices"
```

Claude Code will:
1. Read AGENTS.md
2. Set up Python/TypeScript environment
3. Implement using framework best practices
4. Write scenario tests
5. Create evaluation notebooks
6. Use LangWatch Prompt CLI

**Result**: Production-ready agent with proper testing! 🚀

